*** Settings ***
Library        Collections
Library        HttpLibrary.HTTP
Library        vcslib
Library        Operating System
Resource       ../common_keywords.txt

*** Variables ***

*** Keywords ***

Git clone
  [Documentation]  Clone given repository. Target has the same name than original.
  [Arguments]  ${repo}  ${dir}
  Directory should not exist  ${dir}
  vcslib.Git clone   ${repo}  ${dir}
  Directory should exist  ${dir}
  [Teardown]  Remove directory  ${dir}  recursive=True


Git add
  [Arguments]  ${file}  ${local_repo}
  Directory should exist  ${local_repo}
  ${prev}=  cd  ${local_repo}
  vcslib.Git add  ${file}
  cd  ${prev}

List repository files
  ${files}=  vcslib.Git ls files
  [Return]  ${files}

Cd to temp dir
  [Arguments]  ${dir}=/tmp/
  cd  ${dir}
