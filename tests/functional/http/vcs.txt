*** Settings ***
Library        Collections
Library        HttpLibrary.HTTP
Library        vcslib
Library        Operating System
Resource       ../common_keywords.txt

*** Variables ***

*** Keywords ***

Git clone
  [Documentation]  Clone given repository. Target has the same name than original.
  [Arguments]  ${repo}  ${dir}
  Directory should not exist  ${dir}
  vcslib.Git clone   ${repo}  ${dir}
  Directory should exist  ${dir}


Git add
  [Arguments]  ${file}
  vcslib.Git add  ${file}

Git commit
  [Arguments]  ${file}  ${msg}
  ${data}=  vcslib.Git commit  ${file}  ${msg}
  Log  ${data}

Git push
  [Documentation]  Execute Git push in local directory
  vcslib.Git push


List repository files
  ${files}=  vcslib.Git ls files
  [Return]  ${files}

Cd to temp dir
  [Arguments]  ${dir}=/tmp/
  cd  ${dir}

Svn checkout
  [Arguments]  ${repo}  ${
  Directory should not exist  ${dir}
  vcslib.svn checkout   ${repo}  ${dir}
  Directory should exist  ${dir}

Hg clone
  [Documentation]  Clone given repository. Target has the same name than original.
  [Arguments]  ${repo}  ${dir}
  Directory should not exist  ${dir}
  vcslib.hg clone   ${repo}  ${dir}
  Directory should exist  ${dir}


