[DEFAULT]
# Path to page definition file, relative to this file by default
# You can also use home dir ~/path/to/pages.ini or environment variable $FOO/test.ini
page_definitions = pages/definitions/default.ini
vcs = svn
# Webdriver name: chrome, firefox
browser = chrome
# Seconds to sleep on each page
# Firefox requires value 1, Chrome goes through with 0
request_sleep = 1.0

[general]
# Name of section / setup to test
setup = localhost

[localhost]
host = http://localhost/
user = myaccount
password = GDe43200aeeg

[production]
host = http://production.domain.com/
user = myaccount
password = GDe43200aeeg
vcs = git
