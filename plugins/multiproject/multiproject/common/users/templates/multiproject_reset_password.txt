A ${domain_name} user ${user.getDisplayName()} has requested resetting/changing the password and
as a marked user author you're responsible to do so:

 ${manage_url}

Inform the new password to the user using system notification option or pass the information otherwise.
