scheme.sql  -  always the latest sql-scheme
               You can use this command when migrations makes scheme changes
               mysqldump --host=127.0.0.1 trac_admin --no-data --routines > scheme.sql -u root -p

